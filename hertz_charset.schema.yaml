# Rime schema
# encoding: utf-8

schema:
  schema_id: hertz_charset
  name: "字集过滤·伪方案·勿用"

translator:
  dictionary: hertz_charset
  enable_user_dict: false

key_binder:
  import_preset: default
  custom_bindings:
    __append:
      # 在這裏添加只在本方案中生效的按鍵綁定
  bindings:
    __patch:
      - hertz_tiger:/keys/page_up_down
      - hertz_tiger:/keys/selector
      - hertz_tiger:/keys/options_toggle
